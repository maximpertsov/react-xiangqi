import React from 'react';

import Move from '../../components/Move';

const FullMove = ({ ordering, redMove, blackMove }) => {
  /*
   * display:
   * - ordering
   * - red move
   * - black move if available
   *
   * blocked by:
   * - move must contain id
   */
};

// TODO: proptypes
